git:
        repo: 'https://github.com/libmoon/libmoon.git'
        commit: 'master'

vpp_testing_git:
        repo: 'https://gitlab.lrz.de/vpp-performance/ba-okelmann.git'
        commit: 'master'

# will be installed in this order!
# paths to .deb files to install
dependencies:
        - '/tmp/vpp1.deb'
        - '/tmp/vpp2.deb'

port:
        tx: 1
        rx: 0

cpu-freq: 50
